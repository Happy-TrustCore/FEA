{
  "name": "fea-contact-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that relays the FEA contact form to Brevo. See README.md.",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
